#include <iostream>
using namespace std;

int main()
{
    int n;
    double d;

    cout << "address of n = " << (int)&n << "\tsizeof(n) = " << sizeof(n)  << endl;
    cout << "address of d = " << (int)&d << "\tsizeof(d) = " << sizeof(d) << endl;
}
